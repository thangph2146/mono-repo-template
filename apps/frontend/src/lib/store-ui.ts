/**
 * Lớp dùng chung cho khối form auth storefront — responsive đúng Tailwind
 * (tránh `min-w-[22rem]` gây tràn ngang trên màn nhỏ; breakpoint dùng tiền tố `md:`).
 */
export const STORE_AUTH_FORM_CARD_CLASS =
  "w-full min-w-0 max-w-md sm:max-w-lg mx-auto border-border shadow-level-2";
